# auto-generated by grc.converter

id: clenabled_clAddConst
label: OpenCL Add Const
category: '[OpenCL-Enabled]'

parameters:
-   id: type
    label: Data Type
    dtype: enum
    options: [complex, float, int]
    option_attributes:
        datatype: ['1', '2', '3']
        input: [complex, float, int]
        output: [complex, float, int]
    hide: part
-   id: openCLPlatform
    label: OpenCL Platform Type
    dtype: enum
    options: ['1', '2', '3', '4']
    option_labels: [GPU, Accelerator, CPU, Any]
-   id: devices
    label: OpenCL Device
    dtype: enum
    options: ['1', '2']
    option_labels: [Any, Specific]
    option_attributes:
        hide_specific: [all, '']
-   id: platformId
    label: Platform Id
    dtype: enum
    options: ['0', '1', '2', '3']
    hide: ${ devices.hide_specific }
-   id: deviceId
    label: Device Id
    dtype: enum
    options: ['0', '1', '2', '3']
    hide: ${ devices.hide_specific }
-   id: const
    label: Constant
    dtype: ${ type.input }
    default: '1'
-   id: setDebug
    label: Debug
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']

inputs:
-   domain: stream
    dtype: ${ type.input }

outputs:
-   domain: stream
    dtype: ${ type.output }

templates:
    imports: import clenabled
    make: |-
        % if devices == 1:
        clenabled.clMathConst(${type.datatype},${openCLPlatform},1,0,0,2,${setDebug})
        % else:
        clenabled.clMathConst(${type.datatype},${openCLPlatform},${devices},${platformId},${deviceId},${const},2,${setDebug})
        % endif
    callbacks:
    - set_k(${const})

documentation: |-
    This filter is an OpenCL enabled version of the built-in Add Const block.

    Note that due to the low number of computes per data point, a pure CPU

    approach is actually faster.

file_format: 1
