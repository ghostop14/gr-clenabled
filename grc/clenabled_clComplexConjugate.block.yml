# auto-generated by grc.converter

id: clenabled_clComplexConjugate
label: OpenCL Complex Conjugate
category: '[OpenCL-Enabled]'

parameters:
-   id: openCLPlatform
    label: OpenCL Platform Type
    dtype: enum
    options: ['1', '2', '3', '4']
    option_labels: [GPU, Accelerator, CPU, Any]
-   id: devices
    label: OpenCL Device
    dtype: enum
    options: ['1', '2']
    option_labels: [Any, Specific]
    option_attributes:
        hide_specific: [all, '']
-   id: platformId
    label: Platform Id
    dtype: enum
    options: ['0', '1', '2', '3']
    hide: ${ devices.hide_specific }
-   id: deviceId
    label: Device Id
    dtype: enum
    options: ['0', '1', '2', '3']
    hide: ${ devices.hide_specific }
-   id: setDebug
    label: Debug
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import clenabled
    make: |-
        % if devices == 1:
        clenabled.clMathConst(1,${openCLPlatform},1,0,0,0,4,${setDebug})
        % else:
        clenabled.clMathConst(1,${openCLPlatform},${devices},${platformId},${deviceId},0,4,${setDebug})
        % endif

documentation: |-
    This filter is an OpenCL enabled version of the built-in Complex Conjugate block.

    Note that due to the low number of computes per data point, a pure CPU

    approach is actually faster.

file_format: 1
