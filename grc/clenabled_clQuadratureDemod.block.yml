# auto-generated by grc.converter

id: clenabled_clQuadratureDemod
label: OpenCL Quadrature Demod
category: '[OpenCL-Accelerated]'

parameters:
-   id: openCLPlatform
    label: OpenCL Platform Type
    dtype: enum
    options: ['1', '2', '3', '4']
    option_labels: [GPU, Accelerator, CPU, Any]
-   id: devices
    label: OpenCL Device
    dtype: enum
    options: ['1', '2']
    option_labels: [Any, Specific]
    option_attributes:
        hide_specific: [all, '']
-   id: platformId
    label: Platform Id
    dtype: enum
    options: ['0', '1', '2', '3']
    hide: ${ devices.hide_specific }
-   id: deviceId
    label: Device Id
    dtype: enum
    options: ['0', '1', '2', '3']
    hide: ${ devices.hide_specific }
-   id: setDebug
    label: Debug
    dtype: enum
    options: ['0', '1']
    option_labels: ['Off', 'On']
-   id: gain
    label: Gain
    dtype: float
    default: '1.0'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import clenabled
    make: "% if devices == 1:\n\tclenabled.clQuadratureDemod(${gain},${openCLPlatform},1,0,0,${setDebug})\n\
        \t% else:\n\tclenabled.clQuadratureDemod(${gain},${openCLPlatform},${devices},${platformId},${deviceId},${setDebug})\n\
        \t% endif"

documentation: |-
    This is an OpenCL enabled version of the built-in Quadrature Demod block.

file_format: 1
